# subdomain.to Python SDK

```python
from subdomainto import SubdomainTo

client = SubdomainTo("sdt_live_...")
hostname = client.hostnames.create(project_id, "portal.customer.com", "customer-42-domain")
```
